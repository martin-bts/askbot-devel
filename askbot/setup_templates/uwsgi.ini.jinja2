[uwsgi]
static-map = /m={{ askbot_site }}/static
env = DJANGO_SETTINGS_MODULE={{ askbot_app }}.settings
wsgi-file = {{ askbot_site }}/{{ askbot_app }}/django.wsgi

{# Keep an empty line at EOF so that concatenating files works as expected#}

